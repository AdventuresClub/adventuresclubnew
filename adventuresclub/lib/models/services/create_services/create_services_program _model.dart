class CreateServicesProgramModel {
  String title;
  DateTime startDate;
  DateTime startTime;
  DateTime endTime;
  String description;
  CreateServicesProgramModel(this.title, this.startDate, this.startTime,
      this.endTime, this.description);
}
