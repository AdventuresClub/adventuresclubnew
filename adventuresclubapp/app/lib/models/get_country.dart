class GetCountryModel {
  String country;
  String shortName;
  String flag;
  dynamic code;
  int id;
  String currency;
  GetCountryModel(this.country, this.shortName, this.flag, this.code, this.id,
      this.currency);
}
